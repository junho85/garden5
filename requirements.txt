# Python 3.11 호환 requirements (수정됨)
Django>=4.1,<5.0
pymongo>=4.0
pyyaml>=6.0
slack-sdk>=3.0  # slackclient 대체
# python-markdown-slack  # 임시 제거 (Python 3.11 비호환)
markdown>=3.4  # 최신 markdown 버전 사용
psycopg2-binary>=2.9
supabase>=2.0.0
python-dotenv>=1.0.0
aiohttp>=3.8.0  # slack-sdk RTM 의존성